<%= form_with(model: task, local: true) do |f| %>

    <%= f.label :address %>
    <%= f.text_field :address, autofocus: true %>

    <%= f.label :description %>
    <%= f.text_area :description %>

    <%= f.submit %>
<% end %>