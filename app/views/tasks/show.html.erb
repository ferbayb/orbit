<section class="event-details">
    <div class="details">
        <h2>
            <%= @task.address %>
            <br>
        </h2>
        <h3>
            <%= link_to @task.user.private_name, @task.user %>
            <%= "- Created #{time_ago_in_words(@task.created_at)} ago." %>
        </h3>
        <hr>
        <h5>Task Description</h5>
        <p> <%= @task.description %> </p>
        <section class="admin">
            <%= link_to "Edit", edit_task_path(@task), class: "button" %>
            <%= link_to "Delete",
            task_path(@task),
            class: "button",
            method: :delete,
            data: { confirm: "This action will permanently delete the scan." } %>
        </section>
    </div>
    <div id="showmap">
        <iframe 
            width="450" 
            height="500" 
            style="border:0" 
            loading="lazy" 
            allowfullscreen 
            src= <%= @task.map_embed(@task.latitude, @task.longitude) %> >
        </iframe>
    </div>
    <!-- <%= image_tag "https://maps.googleapis.com/maps/api/staticmap?zoom=16&size=400x300&center=#{@task.latitude},#{@task.longitude}&key=AIzaSyBWcv3SgzJkW2biuy0ii1Tjf-EhCNzhYz8", alt: "Map" %> -->
</section>