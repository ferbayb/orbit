<header>
    <nav>
        <h1>Orb<span class="accent">it</span>.</h1>
        <ul class="left">
            <li> <%= link_to "Tasks", tasks_path %> </li>
            <li> <%= link_to "Users", users_path %> </li>
        </ul>
        <ul class="right">
            <% if current_user %>
                <li>
                    <%= link_to "My Profile", current_user, class: "button" %>
                </li>

                <li>
                    <%= link_to "New Task", new_task_path(@task), class: "button" %>
                </li>

                <li>
                    <%= link_to "Sign Out", tasks_path, class: "button" %>
                </li>
                
            <% else %>
                <li>
                    <%= link_to "Register", tasks_path, class: "button" %>
                </li>

                <li>
                    <%= link_to "Login", tasks_path, class: "button" %>
                </li>
            <% end %>
        </ul>
    </nav>
</header>